{
  "routes": [
    {
      "method": "GET",
      "path": "/why-we",
      "handler": "why-we.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/why-we",
      "handler": "why-we.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/why-we",
      "handler": "why-we.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
