{
  "collectionName": "components_card_cards",
  "info": {
    "name": "Card",
    "icon": "list"
  },
  "options": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "description": {
      "type": "richtext"
    },
    "icon": {
      "model": "file",
      "via": "related",
      "allowedTypes": [
        "images"
      ],
      "plugin": "upload",
      "required": true
    },
    "variant": {
      "type": "enumeration",
      "enum": [
        "small",
        "middle",
        "big"
      ],
      "required": true,
      "default": "middle"
    }
  }
}
